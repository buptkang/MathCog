AG.Interpreter is inference engine with the short memory.

The interpreter takes charge of querying the trace. It uses the undo.framework to 
support redo-undo work.

========================================

Interpreter.cs: IsQuery method